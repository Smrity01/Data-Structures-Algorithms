#include<iostream>
#include<vector>
#include<stdlib.h>
#include<math.h>
using namespace std;
int repeatedNumber(vector<int> A);
int main(){
    vector<int> A;
    
    A.push_back(3);
    A.push_back(4);
    A.push_back(1);
    A.push_back(4);
    A.push_back(1);
    
    cout << repeatedNumber(A);
}
int repeatedNumber(vector<int> A) {
    int ranges = sqrt()
    for(int i = 0; i < A.size(); i++){
        
    }
    return -1;
}